'use client';

import React from 'react';
import ScrollToTop from '@/shared/ui/ScrollToTop';

export const ScrollToTopWrapper: React.FC = React.memo(() => {
  return <ScrollToTop />;
});

ScrollToTopWrapper.displayName = 'ScrollToTopWrapper';
