"use client";

import React from "react";
import NavMenuPagination from "./NavMenuPagination";

const NavMenuOpenDiscounts = () => {
  return <NavMenuPagination filterType="discount" pageSize={8} />;
};

export default NavMenuOpenDiscounts;
